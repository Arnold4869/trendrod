# TrendRod 6.12 修复 + UX 研究笔记

## Bug 修复（"暂无数据" → 5688 行数据）

**症状**：浏览器打开 trendrod 提示"暂无数据，请刷新"，刷新又报"不能打开数据库文件"

**根因**：btrfs ugacl 阻挡
- 宿主机 `/volume1/docker/trendrod/data` owner 是 `root:root`、mode 777
- 看上去 other 可读写，但 btrfs `ugacl` mount 选项 + ACL 阻挡容器内 uid 1000
- 容器 USER `trendrod` (uid 1000) 进不去 → sqlite3 报 `cannot open database file`
- 前端 `_db()` 抛 500 → 主页没数据 → "暂无数据，请刷新"

**修复链**（3 个 commit、3 个版本）：

| 版本 | commit | 改动 |
|---|---|---|
| **0.1.10** | `3049d2f` | Docker 加固（no-new-privileges/cap-drop/read_only/healthcheck） |
| **0.1.11** | `5ef74b0` | 1) 宿主机 `chown -R 1000:1000 data`（btrfs 自动转 owner 到 `Bin:家庭组`）<br>2) main.py 加启动 data 目录可写 check<br>3) `_db()` 包 try/except OperationalError 返回 None |
| **0.1.12** | `2b93f21` | 启动 check 移到 `DB_PATH` 定义之后（修 0.1.11 的顺序 bug） |

**完整 SOP 耗时**：

| 步骤 | 耗时 |
|---|---|
| chown 宿主机 | 1s |
| build 0.1.11 | 6:13 |
| push aliyun | 1:24 |
| pull + up -d | 4s |
| build 0.1.12 | 6:13 |
| push 0.1.12 | 1:24 |
| pull + up -d | 4s |

**完整 SOP 命令**：

```bash
cd /volume1/docker/trendrod
# 改源码后
sed -i 's/trendrod:0.1.X/trendrod:0.1.X+1/' docker-compose.yaml
docker build -t registry.cn-hangzhou.aliyuncs.com/docker-pusher/trendrod:0.1.X+1 \
           -t registry.cn-hangzhou.aliyuncs.com/docker-pusher/trendrod:latest .
docker push registry.cn-hangzhou.aliyuncs.com/docker-pusher/trendrod:0.1.X+1
docker push registry.cn-hangzhou.aliyuncs.com/docker-pusher/trendrod:latest
docker compose pull && docker compose up -d
```

---

## UX 提升研究（cc 明天接手）

**研究规格**：`.review/UX-RESEARCH-2026-06-12.md`（7KB，7 大研究方向）

**启动命令**：
```bash
cd /volume1/docker/trendrod && bash .review/run-ux-research.sh
```

**cc 明天输出位置**：`.review/TRENDROD-UX-PROPOSAL-2026-06-13.md`

**研究覆盖**：
1. 信息架构（IA）— 多组合并存 + 路由分层
2. 视觉系统 — CSS variable 设计 token + 红涨绿跌（中国惯例）
3. 交互细节 — 5 态（加载/错误/成功/空/确认）全覆盖
4. 表格与图表 — Chart.js 增强 + benchmark overlay + drawdown 副图
5. 操作密度 — inline edit + 拖拽 + 快捷键
6. 数据可观测性 — 调仓热力图 + 跨组合对比
7. 性能感知 — skeleton + 增量 cache + web worker

**关键约束**：
- 不改后端 main.py
- 不直接改代码
- 方案要分 Phase、标风险、引用现有 50 个 issue
- 红涨绿跌（与国际相反）

---

## 待办（6.13）

- [ ] 跑 `bash .review/run-ux-research.sh` 让 cc 出方案
- [ ] 看方案、决定做哪些 Phase
- [ ] 玻璃安装验收（执手磨蹭是否处理）
